import React from 'react';

// Mendefinisikan komponen Display
const Display = ({ value }) => {
  return <div>Display Value: {value}</div>;
};

// Ekspor default komponen Display
export default Display;
import { Display } from './display';